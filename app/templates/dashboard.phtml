<?php $db = Services::getDBConnection() ?>
<?php $users = $db->query('SELECT * FROM points'); ?>
<div class="container">
    <table id="points">
        <thead>
        <tr>
            <th>Фамилия</th>
            <th>Плюс</th>
            <th>Минус</th>
            <th>Баланс</th>
            <th>Потрачено</th>
        </tr>
        </thead>
        <tbody>
        <?php foreach($users as $row) : ?>
            <tr>
                <td><?php echo $row['last_name'] ?></td>
                <td><?php echo $row['available'] ?></td>
                <td><?php echo $row['points'] ?></td>
                <td><?php echo $row['used'] ?></td>
                <td><?php echo $row['used'] ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>